services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ${MBOX_DIR:-~/Downloads}:/data
      - email-db:/app/data
    environment:
      - MBOX_PATH=${MBOX_PATH:-}
      - MAILBOX_NAME=${MAILBOX_NAME:-}

volumes:
  email-db:

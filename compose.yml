services:
  app:
    build: .
    container_name: mailrewind
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - ${MBOX_DIR:-~/Downloads}:/data
      - mailrewind-db:/app/data

volumes:
  mailrewind-db:
